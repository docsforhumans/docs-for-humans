import styled from 'styled-components'

export const Nav = styled.nav`
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  width: 100%;
  a {
    padding: 1em;
    cursor: pointer;
  }
`
